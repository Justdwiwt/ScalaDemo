package leetCode

object Solution_1649 {
  //  def createSortedArray(instructions: Array[Int]): Int = (instructions
  //    .zipWithIndex
  //    ./:((collection.immutable.TreeSet.empty[(Int, Int)], 0L)) {
  //      case ((items, acc), (el, idx)) => (items + (el -> idx), acc + items.rangeUntil((el, 0)).size.min(items.rangeFrom((el + 1, 0)).size))
  //    }._2 % (1e9 + 7).toLong)
  //    .toInt
}
