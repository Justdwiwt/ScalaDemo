package leetCode

object Solution_535 {

  class Codec {
    // Encodes a URL to a shortened URL.
    def encode(longURL: String): String = longURL

    // Decodes a shortened URL to its original URL.
    def decode(shortURL: String): String = shortURL
  }

}
